Screenshots for the fix/font-sans-resolves pull request (before = main, after = the PR), rendered
from next dev with every /api call stubbed with synthetic data. Not code; never merged.
Safe to delete once the owner has decided on that PR.
